# /Users/a1111/work_space/elastic-search-demo/sites-hub/scripts/../conf/nginx.conf
# 本地开发用 nginx 配置（VPS 用 deploy-vps.sh 生成的 sites-hub.conf）。
#
# **本文件由 scripts/render-nginx-conf.sh 自动生成**。
# 子站 location 由 sites-hub/scripts/sites.sh 驱动（**唯一真相源**）。
# 任何新增站点只需改 sites.sh + 一张首页卡片 + 1 个项目目录，再跑本脚本。
# T2 安全响应头在此集中配置。

worker_processes  auto;
error_log  logs/error.log;
pid        logs/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /opt/homebrew/etc/nginx/mime.types;
    default_type  application/octet-stream;
    access_log    logs/access.log;

    sendfile        on;
    keepalive_timeout  65;

    # T13：gzip 补全
    gzip  on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/javascript text/xml
               application/javascript application/json application/xml
               application/xhtml+xml application/rss+xml application/atom+xml
               application/ld+json application/manifest+json application/wasm
               image/svg+xml image/x-icon image/bmp image/vnd.microsoft.icon
               font/woff2 font/ttf font/otf;

    # T3：basic auth 暴力破解限流
    limit_req_zone $binary_remote_addr zone=auth:10m rate=10r/m;


    # T11：健康检查（auth 前）
    server {
        listen       8081;
        server_name  localhost;
        charset      utf-8;

        # T2：安全响应头（即使 4xx/5xx 也带上 always）
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-Frame-Options        "DENY" always;
        add_header Referrer-Policy        "strict-origin-when-cross-origin" always;
        add_header Permissions-Policy     "geolocation=(), camera=(), microphone=(), interest-cohort=()" always;
        add_header Content-Security-Policy "default-src 'self';             img-src 'self' data: blob:;             style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;             font-src 'self' https://fonts.gstatic.com;             script-src 'self' 'unsafe-inline';             frame-ancestors 'none'; base-uri 'self'; form-action 'self'; report-uri /csp-report;" always;
        # HTTPS-only 站点上再加 HSTS（本地开发不上）：见 deploy-vps.sh

        # T3：限流（auth 路径；trusted IP 旁路）
        limit_req zone=auth burst=20 nodelay;

        # 健康检查（auth 前）
        location = /healthz {
            access_log off;
            return 200 "ok\n";
            add_header Content-Type text/plain;
        }

        # T11：Prometheus exporter 取数（仅 localhost，防外泄）
        location = /metrics {
            access_log off;
            allow 127.0.0.1;
            allow ::1;
            deny  all;
            stub_status;
        }

        # T11：basic auth 探测（200 表示 auth 后端正常）
        location = /auth-check {
            access_log off;
            return 200 "auth-ok\n";
            add_header Content-Type text/plain;
        }

        # T15：CSP violation report（写日志，供后续 ELK/Loki ingest）
        location = /csp-report {
            access_log logs/csp-report.log combined buffer=32k flush=5s;
            return 204;
        }

        # ===== 自定义错误页（T6）=====
        error_page 404 /404.html;
        error_page 500 502 503 504 /50x.html;

        # ===== 长缓存 VitePress 哈希资源（T6）=====
        location ~* "^/[^/]+/assets/.*\.(js|css|woff2|svg|png|webp|avif|ico)$" {
            add_header Cache-Control "public, max-age=31536000, immutable";
            access_log off;
            try_files $uri =404;
        }

        # ===== 导航首页 =====
        location / {
            root   www/;
            index  index.html;
            try_files $uri $uri/ =404;
            autoindex off;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /es { return 301 /es/; }
        location /es/ {
            alias  ../es-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /mysql { return 301 /mysql/; }
        location /mysql/ {
            alias  ../mysql-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /redis { return 301 /redis/; }
        location /redis/ {
            alias  ../redis-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /cloud { return 301 /cloud/; }
        location /cloud/ {
            alias  ../springcloud-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /python { return 301 /python/; }
        location /python/ {
            alias  ../python-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /kafka { return 301 /kafka/; }
        location /kafka/ {
            alias  ../kafka-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /java { return 301 /java/; }
        location /java/ {
            alias  ../java-web-manual/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /tools { return 301 /tools/; }
        location /tools/ {
            alias  ../tools-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /frontend { return 301 /frontend/; }
        location /frontend/ {
            alias  ../frontend-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /linux { return 301 /linux/; }
        location /linux/ {
            alias  ../linux-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /cloud-native { return 301 /cloud-native/; }
        location /cloud-native/ {
            alias  ../cloud-native-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /ai { return 301 /ai/; }
        location /ai/ {
            alias  ../ai-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /bigdata { return 301 /bigdata/; }
        location /bigdata/ {
            alias  ../bigdata-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /network { return 301 /network/; }
        location /network/ {
            alias  ../network-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /video { return 301 /video/; }
        location /video/ {
            alias  ../video-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /filesystem { return 301 /filesystem/; }
        location /filesystem/ {
            alias  ../filesystem-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /java-language { return 301 /java-language/; }
        location /java-language/ {
            alias  ../java-language-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /architecture { return 301 /architecture/; }
        location /architecture/ {
            alias  ../architecture-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /system-design { return 301 /system-design/; }
        location /system-design/ {
            alias  ../system-design-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /postgresql { return 301 /postgresql/; }
        location /postgresql/ {
            alias  ../postgresql-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /observability { return 301 /observability/; }
        location /observability/ {
            alias  ../observability-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /security { return 301 /security/; }
        location /security/ {
            alias  ../security-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /devops { return 301 /devops/; }
        location /devops/ {
            alias  ../devops-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /rust { return 301 /rust/; }
        location /rust/ {
            alias  ../rust-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /go { return 301 /go/; }
        location /go/ {
            alias  ../go-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /clickhouse { return 301 /clickhouse/; }
        location /clickhouse/ {
            alias  ../clickhouse-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /design-pattern { return 301 /design-pattern/; }
        location /design-pattern/ {
            alias  ../design-pattern-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }

        # T1：自动生成自 sites.sh（勿手动改）
        location = /chaos { return 301 /chaos/; }
        location /chaos/ {
            alias  ../chaos-html/.vitepress/dist/;
            index  index.html;
            try_files $uri $uri.html $uri/index.html =404;
        }
    }
}
